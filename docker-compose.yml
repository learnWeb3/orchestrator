name: orchestration-agent-dev

services:
  mongodb:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - orchestration_agent_mongo_data:/data/db

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  orchestration_agent_mongo_data:
